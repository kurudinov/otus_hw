apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-auth-app
  namespace: otus-hw6-anton
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: my-auth-app
  template:
    metadata:
      labels:
        app: my-auth-app
    spec:
      automountServiceAccountToken: false
      containers:
        - name: my-auth-app
          image: antonkurudinov/otus_hw6_auth:1.7
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 8001
              name: my-auth-port
          livenessProbe:
            httpGet:
              port: 8001
              path: /health
            initialDelaySeconds: 10
            periodSeconds: 10
          envFrom:
            - configMapRef:
                name: db-settings
            - secretRef:
                name: db-secret

