const deliveryModel = require('../models/deliveryModel');

// get xz
// exports.getXz = function (req, res, next) {
//     if (!req.user || !req.user.id) {
//         res.status(401).json({ message: "Not authorized. User context not provided" });
//         return;
//     }

    
// };